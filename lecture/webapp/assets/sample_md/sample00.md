# 第0回 サンプル実験

## Sample: 環境確認：gcc --version（イメージ）

### Code
```c
// 端末（PowerShell）で次を実行して確認します。
// gcc --version

// このツール内ではコマンド実行はしません。
```

### Trace (exported reference)
```json
[
  {
    "stdout": "$ gcc --version\ngcc ...\n",
    "vars": {
      "gcc": "installed"
    }
  }
]
```
